# Database Configuration for IoT Destilasi
# Copy this file to .env and adjust the values as needed

# MySQL Database Settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=iot_desalinasi
DB_PORT=3306

# Server Configuration
PORT=3000

# Environment Mode
# - production: Database is REQUIRED. Server will fail if database is unavailable.
# - development: Auto-fallback to mock data if database is unavailable.
NODE_ENV=development

# Optional: Uncomment if you need custom settings
# DB_POOL_MAX=5
# DB_POOL_MIN=0
# DB_POOL_ACQUIRE=30000
# DB_POOL_IDLE=10000

